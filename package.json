{
    "private": true,
    "type": "module",
    "scripts": {
        "dev:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-lang-switch.css --watch",
        "dev:scripts": "node bin/build.js --dev",
        "build:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-lang-switch.css --minify",
        "build:scripts": "node bin/build.js",
        "dev": "npm-run-all --parallel dev:*",
        "build": "npm-run-all build:*",
        "prettier": "npx prettier --write ."
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.1.10",
        "esbuild": "^0.25.0",
        "esbuild-postcss": "^0.0.4",
        "npm-run-all": "^4.1.5",
        "prettier": "^3.1.1",
        "prettier-plugin-blade": "v2.0.0",
        "prettier-plugin-tailwindcss": "^0.6.10",
        "tailwindcss": "^4.1.14"
    }
}
